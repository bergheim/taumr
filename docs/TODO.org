#+title: taumr TODO
#+TODO: TODO INPROGRESS WAITING BLOCKED | DONE CANCELLED
#+STARTUP: overview

* TODO Fork Pi context into a Herdr worktree  :herdr:pi:fork:worktree:
:PROPERTIES:
:ID:       20260828T112027Z-7986ce
:CREATED:  [2026-08-28 Fri 13:20]
:END:
One command from a Herdr-hosted Pi session produces a grouped worktree workspace whose agent already has this conversation. Live spike: =~/.pi/agent/extensions/herdr-fork= (22 unit tests). Land the portable copy here under =.pi/agent/extensions/= after a disposable-host-repo smoke — /never/ against =~/.pi=.

Command: =/herdr-fork [focus]=

Focus is both the branch slug /and/ the compact prompt. No extra flags. Do not overload Pi =/fork= (rewind to an old user message) or =/clone= (replace /this/ TUI).

Operator sees:
- This pane stays on this session, cwd, and branch. No compact, no =/clone=, no =/fork=, no cwd change, no pane close. The invocation itself appends to this session JSONL — expected.
- A new Herdr workspace grouped under the parent repo.
- A real git worktree on a new branch based at the captured source commit.
- That workspace's Pi already has the conversation (full, or a compact of it after the compact gate).

Flow:
1. Refuse unless ~HERDR_ENV=1~, socket and pane id set, =herdr= CLI can reach the server, this Pi is idle, ~getSessionFile()~ is a readable regular file, current leaf equals the last persisted leaf, and the project is a clean host-visible git repo. Resolve repo root from the project cwd. Refuse =~/.pi= by =realpath= of the git common dir (/not/ a string match). Refuse containers. On dirty: list the porcelain paths and stop. No stash/commit path. Host-only — no =just wt=, no in-container tmux. Worktrees land where Herdr puts them (=~/.herdr/worktrees=).
2. Capture source session path and =git rev-parse HEAD=. That OID is create =--base= (do not race a moving =HEAD=). Slug from focus text, else timestamp/short-hash. Sanitize: legal git branch, length cap, no leading ~-~, no ~..~, no trailing ~.lock~. Reject focus with newlines or an extra leading ~/~.
3. =herdr worktree create --cwd <root> --branch <branch> --base <oid> --label <short> --no-focus=
   /Never/ =--focus=. Do not pre-check branch uniqueness (TOCTOU); let create fail on collision.
4. Parse JSON. Do not predict ids. Treat exit status, stdout envelope, and stderr separately. Schema-validate workspace id, checkout path, root pane id. If the shape is wrong, print raw stdout/stderr after extracting any ids that /are/ present. Validate root pane cwd equals checkout path (=agent start= has no cwd flag). Poll until that pane is an interactive shell — create returning is /not/ shell-ready.
5. =herdr agent start <unique-name> --kind pi --pane <root-pane-id> -- --fork <source-session-path>=
   Argv arrays, no shell interpolation. Extra args after =--= reach =pi=.
6. Wait with timeout for idle or blocked (=--until idle= alone never returns on blocked). A brand-new checkout is untrusted: start-ok may be the project-trust prompt. Tell the operator to accept it in that pane. Do not send keys or =agent prompt= through it. If start times out: =herdr agent get/read= before printing a rerun — Pi may already occupy the pane.
7. If focus text: until a scratch-pane test proves =herdr agent prompt= runs =/compact= as a command, print the =/compact= line and do /not/ send it. After that test: prompt the clone only, =--wait --timeout=, and confirm a new compaction entry in the clone JSONL. /Never/ compact the source.
8. Report branch, checkout path, workspace id, pane id, agent name, new session id/path, compact status, and the shared =.git= caveat. /Never/ auto-delete a successful checkout.

Invariants:
- *Source:* same session id, same cwd, same branch; no compaction entry; no rewind; no pane close. /Not/ bit-wise JSONL equality.
- Isolation is worktree-local. Clone shares =.git= (refs, stash, hooks, config). A clone-side =git stash=, =git branch -D=, rebase, or =git config= can hit the source.
- =git worktree add= is invisible to Herdr. Only =herdr worktree create= or =open= make a grouped workspace.
- =worktree create= produces a workspace + shell. It does not start Pi and does not copy a session.
- =pi --fork= copies the whole JSONL and restores the last persisted leaf, /not/ ~getLeafId()~. Refuse =/tree= away from tip.
- ~isPersisted()~ only means persistence is enabled. Need a real session file. Once idle, Pi writes synchronously — that is the flush.

Failure safety: source pane is /sacred/ on every path, including errors after create. Create-ok / start-fail: keep the worktree, always print =herdr worktree remove --workspace <id>=, print =agent start= recovery only if the pane is still a shell.

Non-goals: uncommitted files in the clone; devcontainer / =just wt= / worktrunk / =@yassimba/pi-herdr-worktree=; Pi =/fork= rewind; reuse of an existing worktree; auto-close source; merge-back; flags that split branch name from compact prompt; editing =herdr-agent-state.ts=.

Success: one command, visible grouped workspace, usable inherited conversation, source invariant holds. Smoke on a disposable host git repo first. Then copy into this kit and add the fork path to =docs/workflow.md=.

* TODO /watch sibling: steer, ntfy, abort loops  :herdr:pi:watch:steer:
:PROPERTIES:
:ID:       20260828T113951Z-378ec6
:CREATED:  [2026-08-28 Fri 13:39]
:END:
Sibling Herdr pane. Not a worktree fork. Same cwd as the source. Fresh Pi: your brief plus tails of source JSONL and git diff. It does /not/ =pi --fork= the looping session.

Command (on the source): =/watch [brief]=
Example: =/watch stop if it touches ~/.pi=

Opt-in per run. No watcher unless you type it.

*Sees* (all of these):
- Source session JSONL (messages, tool calls, compaction).
- Git worktree diff (=~git diff --stat= / no net progress).
- Source pane PTY via =herdr agent read= (trust prompts, spinner stalls).
- The watch brief.

*Does* without asking:
- *Inject* a steering line into the source session via JSONL append / RPC. /Not/ =herdr agent prompt= typing (compact-via-prompt risk; also looks like you only if the TUI sent it). Spike the actual Pi write path; refuse to ship on a fake API.
- *ntfy* when it thinks you should look (after inject, before abort).
- *Abort:* watcher picks depth. Tight loop → rewind last turn. Bad streak → rewind K turns. Run is junk → =/clear=, inject a different prompt, start it again.

Ladder on a detected circle: *inject → ntfy → abort*. First hit steers. Ping so you can grab the wheel. If it keeps circling, rewind/clear.

*Circle signals* (all live; knobs later):
- Same tool+args N times (JSONL).
- File churn / no net git progress in T minutes.
- Brief violation (immediate fire, not necessarily a loop).
- Frontier judgment on the tail (slow drift). Watcher model is a *frontier critic always*, not the local worker.

Operator sees:
- Source pane keeps running. =/watch= opens a sibling agent in the same Herdr workspace, same worktree.
- Watcher pane shows brief + tails, then inject/ntfy/abort actions as they happen.
- Source JSONL gains steering messages that did not come from the keyboard.
- On abort: last turn (or K) gone, or a cleared session with a new prompt already starting.

Flow:
1. Source is a Herdr Pi, idle enough to accept =/watch=. Refuse outside Herdr. Same host-visible git cwd. Watcher is /not/ a =herdr worktree create=.
2. Capture source session path, pane/agent id, cwd, =HEAD=. Open sibling: =herdr agent start= (or split) in this workspace, =--kind pi=, cwd unchanged. Seed watcher with brief + last ~20 JSONL events + =git diff --stat=.
3. Watcher loop (heuristic, not every token): read JSONL, diff, =herdr agent read=. Match N-times / churn / brief / judgment.
4. On hit: append steering message onto *source* session via RPC/JSONL. Then ntfy. Keep watching.
5. If still circling: pick rewind 1, rewind K, or =/clear= + new prompt + start. Then ntfy what it did.
6. Source pane stays up. Watcher does not close it. Watcher may stop itself when source goes idle and stays clean.

Invariants:
- Watcher session ≠ source session. No shared leaf, no =pi --fork=.
- Same worktree, shared =.git=. Watcher reads; it does not commit, stash, or =git reset= to “help”.
- Inject is a real session event, not a PTY keystroke.
- Abort never deletes the worktree or the source pane.
- =/watch= without a brief is allowed but weaker (heuristics only).

Non-goals: always-on watcher for every agent; same-session extension that shares fate with the runaway; =herdr agent prompt= as the inject path; host sidecar with no pane; Emacs overlay; watching live =~/.pi= as a default target; merging with =/herdr-fork= (fork isolates work; watch criticises it).

Open (answer in the TODO log during impl):
- Exact Pi RPC / JSONL-append / rewind / =/clear= primitives on the installed Pi version. If missing, that spike /is/ the first cut — do not fake it with TUI typing.
- Defaults for N and T.
- ntfy topic (reuse Pi completion topic or a =watch= topic).
- How the watcher authenticates a write into another session’s JSONL without racing the live TUI.

Success: =/watch …= from a Herdr Pi opens a frontier sibling that can steer the source via session write, ping you, and rewind or clear+restart a circling run — without touching the source pane’s life cycle and without forking a new worktree.

Land in this kit as a Pi extension (source =/watch=) plus a tiny Herdr sibling recipe. Document next to the fork path in =docs/workflow.md=.

* DONE Vendor pi-taumr-statusbar                               :pi:statusbar:
CLOSED: [2026-08-28 Fri 12:54]
:PROPERTIES:
:ID:       20260828T125429Z-380298
:CREATED:  [2026-08-28 Fri 12:54]
:LAST_AGENT: unknown
:END:
:LOGBOOK:
- Session unknown [2026-08-28 Fri 12:54]
:END:
Copy ~/.pi/agent/extensions/usage into .pi/agent/extensions/pi-taumr-statusbar with behavior preserved.

* DONE Explore replacement taumr logos                      :design:branding:
CLOSED: [2026-08-28 Fri 17:01]
:PROPERTIES:
:ID:       20260828T165533Z-0c83c6
:CREATED:  [2026-08-28 Fri 16:55]
:LAST_AGENT: unknown
:END:
:LOGBOOK:
- Note taken on [2026-08-28 Fri 17:01] \\
  Six transparent logo concepts saved under assets/logo-concepts; existing logo preserved.
- Session unknown [2026-08-28 Fri 17:01]
:END:
Create six transparent logo concepts: three image-generator explorations and three independent Claude SVG concepts. Preserve the existing mark until selection.

* DONE Riff fray-ragged logo concepts                       :design:branding:
CLOSED: [2026-08-28 Fri 18:17]
:PROPERTIES:
:ID:       20260828T181720Z-a1ae75
:CREATED:  [2026-08-28 Fri 18:17]
:LAST_AGENT: unknown
:END:
:LOGBOOK:
- Note taken on [2026-08-28 Fri 18:17] \\
  Six transparent riffs saved under assets/logo-concepts; README uses logo-fray-ragged.svg.
- Session unknown [2026-08-28 Fri 18:17]
:END:
Create six new transparent logo riffs based directly on assets/logo-fray-ragged.svg and keep README pointed at that asset.
